name: Smoke Test

on:
  push:
    branches: 
      - main

jobs:
  test-runner:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Test Runner
        run: echo "GitHub Actions working fine"
